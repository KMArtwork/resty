import App from "../App";
import { render, screen, fireEvent } from '@testing-library/react';
// import '@testing-library/jest-dom';

describe('Testing App component...', () => {

  test('App component should be visible', () => {
    let test = 1;
    expect(test).toBe(1);
    // render(<App />);

    // const callApi = jest.fn();
    // const setData = jest.fn();
    // const setRequestParams = jest.fn();
  
    // let requestParams = {
    //   method: 'GET',
    //   url: 'test.url.com',
    //   body: 'test body',
    // }

    // let appEl = screen.getByTestId('app');
    // expect(appEl).toBeVisible();
  })

})